import { readFileSync, writeFileSync } from "node:fs";

const path = ".automation/apply-canvas-primary-gesture-fix.mjs";
let source = readFileSync(path, "utf8");
const oldBlock = `board = replaceOnce(\n  board,\n  \`      const drawingSession = drawingSessionRef.current;\\n      if (\`,\n  \`      const primaryCanvasPointerCandidate =\\n        primaryCanvasPointerCandidateRef.current;\\n      if (\\n        primaryCanvasPointerCandidate !== null &&\\n        primaryCanvasPointerCandidate.pointerId === event.pointerId &&\\n        Math.hypot(\\n          event.clientX - primaryCanvasPointerCandidate.startPoint.x,\\n          event.clientY - primaryCanvasPointerCandidate.startPoint.y,\\n        ) > rightDoubleClickDistancePx\\n      ) {\\n        primaryCanvasPointerCandidateRef.current = null;\\n        clearPendingPrimaryCanvasTap();\\n      }\\n\\n      const drawingSession = drawingSessionRef.current;\\n      if (\`,\n  "pointer move arbitration",\n);`;
const newBlock = `const pointerMoveStart = board.indexOf(\n  "    const handlePointerMove = (event: PointerEvent) => {",\n);\nconst pointerMoveDrawingAnchor = board.indexOf(\n  "      const drawingSession = drawingSessionRef.current;\\n      if (",\n  pointerMoveStart,\n);\nif (pointerMoveStart < 0 || pointerMoveDrawingAnchor < 0) {\n  throw new Error("Missing pointer move arbitration anchor");\n}\nconst pointerMoveSearch =\n  "      const drawingSession = drawingSessionRef.current;\\n      if (";\nconst pointerMoveReplacement =\n  \`      const primaryCanvasPointerCandidate =\\n        primaryCanvasPointerCandidateRef.current;\\n      if (\\n        primaryCanvasPointerCandidate !== null &&\\n        primaryCanvasPointerCandidate.pointerId === event.pointerId &&\\n        Math.hypot(\\n          event.clientX - primaryCanvasPointerCandidate.startPoint.x,\\n          event.clientY - primaryCanvasPointerCandidate.startPoint.y,\\n        ) > rightDoubleClickDistancePx\\n      ) {\\n        primaryCanvasPointerCandidateRef.current = null;\\n        clearPendingPrimaryCanvasTap();\\n      }\\n\\n      const drawingSession = drawingSessionRef.current;\\n      if (\`;\nboard =\n  board.slice(0, pointerMoveDrawingAnchor) +\n  pointerMoveReplacement +\n  board.slice(pointerMoveDrawingAnchor + pointerMoveSearch.length);`;
if (!source.includes(oldBlock)) throw new Error("Missing script block to repair");
source = source.replace(oldBlock, newBlock);
writeFileSync(path, source);
