verdict: approved
findings: []
criteria_checked:
  - Acceptance criterion 1
remaining_risks: []
confidence: high

# Blocking finding example:
# findings:
#   - severity: P1
#     invariant_id: PERSIST-004
#     location: src/modules/local-persistence/save.ts:42
#     issue: Concurrent retries can create duplicate revisions.
#     failure_scenario: A timeout occurs after the server commits but before the client receives the response.
#     evidence: The retry uses no durable idempotency key.
#     verification_gap: No repository test repeats the same save request.
#     smallest_fix: Persist and reuse one operation identity for all attempts.

