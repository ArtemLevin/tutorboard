name: CI

on:
  pull_request:

permissions:
  contents: write

jobs:
  format:
    name: Format PR5 type guard
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/coordinate-plot-editor
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - name: Apply repository Prettier
        run: npx prettier --write src/app/CoordinatePlotEditorWorkflow.test.tsx
      - name: Restore production workflow and push
        run: |
          git show origin/main:.github/workflows/ci.yml > .github/workflows/ci.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: format coordinate plot workflow guard"
          git push origin HEAD:feature/coordinate-plot-editor
