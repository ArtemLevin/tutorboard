name: CI

on:
  pull_request:

permissions:
  contents: write

concurrency:
  group: ci-native-background-selection-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  apply-native-background-selection:
    name: Apply native background selection listener
    if: github.head_ref == 'feature/minimal-board-chrome'
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Check out feature branch
        uses: actions/checkout@v7
        with:
          ref: feature/minimal-board-chrome
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Apply native listener fix
        run: python scripts/apply-native-background-selection-listener.py

      - name: Format product change
        run: npx prettier --write src/adapters/canvas-konva/BoardStage.tsx

      - name: Verify product change
        run: |
          npx prettier --check src/adapters/canvas-konva/BoardStage.tsx
          npm run lint
          npm run typecheck

      - name: Install target browsers
        run: npx playwright install --with-deps chromium firefox

      - name: Build production bundle
        env:
          VITE_FEATURE_SERVER_SYNC: "true"
          VITE_GEOMETRYOS_BASE_URL: http://127.0.0.1:4173/geometryos
        run: npm run build

      - name: Run selection contract in Chromium
        run: npm run e2e -- tests/e2e/selection.spec.ts --project=chromium

      - name: Run selection contract in Firefox
        run: npm run e2e -- tests/e2e/selection.spec.ts --project=firefox

      - name: Commit product fix
        run: |
          if git diff --quiet -- src/adapters/canvas-konva/BoardStage.tsx; then
            echo "Product fix is already applied."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/adapters/canvas-konva/BoardStage.tsx
          git commit -m "fix: listen for empty-canvas selection natively"
          git push origin HEAD:feature/minimal-board-chrome
