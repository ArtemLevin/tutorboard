name: Fix minimal board E2E selectors

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: fix-minimal-board-e2e-selectors
  cancel-in-progress: false

jobs:
  fix:
    if: github.head_ref == 'feature/minimal-board-chrome'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/minimal-board-chrome
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: python scripts/fix-minimal-board-e2e-selectors.py
      - run: npm ci
      - run: npm run format
      - run: npm run typecheck
      - name: Commit selector fixes
        run: |
          rm -f \
            .github/workflows/fix-minimal-board-e2e-selectors.yml \
            scripts/fix-minimal-board-e2e-selectors.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test: align board E2E paths with minimal chrome"
          git push origin HEAD:feature/minimal-board-chrome
